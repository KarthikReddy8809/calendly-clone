# Optional. When omitted, the app uses the Vite dev proxy at /api/v1.
# Set this for production builds that talk to a remote API origin.
# Example: https://calendly-clone-server.vercel.app/api/v1
VITE_API_BASE_URL=https://calendly-clone-server.vercel.app/api/v1
